import java.io.File;

public class LineTree {

    ScopeNode root;

    LineTree(File sjavaFile){
        parser(sjavaFile);
    }

    /**
     * method which parse the sjava file to a tree of nested scopes which is easier to analyze.
     * @param sjavaFile the original file we parse
     */
    private void parser(File sjavaFile){

    }
}
